{
  "name": "microflashcards",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "npx serve out",
    "lint": "next lint",
    "railway:build": "next build && next export",
    "railway:start": "npx serve -s out -p ${PORT:-8080}",
    "vercel-build": "next build && next export"
  },
  "dependencies": {
    "framer-motion": "^6.5.1",
    "next": "^12.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}